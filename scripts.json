{
  "scripts": {
    "install:gustwind": "deno install -A --unstable --no-check -f https://deno.land/x/gustwind@v0.17.0/cli.ts",
    "build": "gustwind -b",
    "start": "gustwind -d",
    "ci:build": "deno run -A --unstable --no-check https://deno.land/x/gustwind@v0.17.0/cli.ts -b"
  }
}
